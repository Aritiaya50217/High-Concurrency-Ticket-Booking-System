name: CI/CD Pipeline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build-test:
    name: Build & Test High-Concurrency-Ticket-Booking-System
    runs-on: ubuntu-latest

    steps:
      # ----------------------------
      # Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ----------------------------
      # Debug: ตรวจสอบไฟล์
      - name: Debug service folder
        run: |
          echo "Root folder: $PWD"
          ls -la

      # ----------------------------
      # Setup Go
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.24.x"

      # ----------------------------
      # Download dependencies
      - name: Go mod tidy & download
        run: |
          cd user-service
          go mod tidy
          go mod download

      # ----------------------------
      # Run unit tests
      - name: Run unit tests
        run: |
          cd user-service
          go test ./internal/... -v -race

      # ----------------------------
      # Build binary
      - name: Build binary
        run: |
          cd user-service
          go build -v -o service ./cmd/server

  docker:
    name: Docker Build & Push
    needs: build-test
    runs-on: ubuntu-latest

    steps:
      # ----------------------------
      # Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ----------------------------
      # Debug Docker secrets
      - name: Debug Docker username
        run: echo "DOCKER_USERNAME=${{ secrets.DOCKER_USERNAME }}"

      # ----------------------------
      # Docker login
      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # ----------------------------
      # Build Docker image
      - name: Build Docker image
        run: |
          cd user-service
          docker build -t ${{ secrets.DOCKER_USERNAME }}/high-concurrency-ticket-booking-system:latest .
          docker tag ${{ secrets.DOCKER_USERNAME }}/high-concurrency-ticket-booking-system:latest \
                     ${{ secrets.DOCKER_USERNAME }}/high-concurrency-ticket-booking-system:${{ github.sha }}

      # ----------------------------
      # Push Docker image
      - name: Push Docker image
        run: |
          cd user-service
          docker push ${{ secrets.DOCKER_USERNAME }}/high-concurrency-ticket-booking-system:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/high-concurrency-ticket-booking-system:${{ github.sha }}
 